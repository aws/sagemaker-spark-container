"""Default config files -- a convenience for local development."""
from typing import Any, Dict

default_resource_config: Dict[str, Any] = {"current_host": "algo-1", "hosts": ["algo-1"]}
default_processing_job_config: Dict[str, Any] = {}
